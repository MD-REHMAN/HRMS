app.controller('viewPaySlipCtrl', ['$scope', '$rootScope', '$location', '$http', 'localStorageService', 'userService', function ($scope, $rootScope, $location, $http, localStorageService, userService) {
    /*
    $scope.message = "";
    $scope.expand = "?$expand=CompanyBranch,UserDepartments/Department,PaySlips2/SalaryComponentAmounts/SalaryComponent&$filter=UserID eq '" + $rootScope.selectedPayslip.UserID + "'";

    userService.getUser($scope.expand).then(function (response) {
        $scope.viewpaySlipData = response.data.value;
    }, function (error) {
        $scope.message = $scope.message + "- Invalid List -";
    });
    */

}]);